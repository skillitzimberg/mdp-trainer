// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: "AIzaSyBvLCN7pi_R22hSDVAYpNcB-xOGyVYt0SA",
    authDomain: "mdp-trainer.firebaseapp.com",
    databaseURL: "https://mdp-trainer.firebaseio.com",
    projectId: "mdp-trainer",
    storageBucket: "mdp-trainer.appspot.com",
    messagingSenderId: "495140600182",
    appId: "1:495140600182:web:63a10d3f95af5fbca0a257",
    measurementId: "G-DDEK41FPCL"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
